{
  "name": "better-metrics",
  "version": "1.0.0",
  "description": "Hospitable reservation metrics dashboard",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.99.0"
  }
}
